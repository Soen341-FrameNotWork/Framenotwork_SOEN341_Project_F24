-- student name,s_id
-- given t_id

-- how many paper:


-- select S.id, S.s_name from students S
-- join (SELECT student_id FROM team_student WHERE team_id = ?) AS student_ids
-- on S.s_id = student_ids.student_id;


-- -- ratings for current student.

-- select R.*, S.s_name, S.s_id from Students,
-- (select * rating from ratings where ratings.reviewee_id = ? and ratings.team_id = ?) as R
-- WHERE R.reviewer_id = S.s_id;

